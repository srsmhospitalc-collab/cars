rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, update: if request.auth.uid == userId;
      allow create: if request.auth != null;
    }
    match /redeem_requests/{doc} {
      allow create: if request.auth != null;
      allow read: if false;
    }
  }
}
